import HomeClient from "./HomeClient";

export const metadata = {
  title: "DropPR.ai - Performance PR for AI discovery",
  description: "Turn one video into a published article on 1,000+ media sites. Stop Renting Attention. Start Owning Authority.",
  keywords: "video to article generation, AI article generation, press release distribution, Drop PR, PR platform, content publishing, SEO indexing, affiliate article publishing, brand authority builder",
};

export default function Home() {
  return <HomeClient />;
}
